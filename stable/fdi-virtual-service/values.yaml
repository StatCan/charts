# Default values for virtual service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Globals
nameOverride: "fdi-dotstatsuite-tenants-vss"
fullnameOverride: "fdi-dotstatsuite-tenants-vss"
#virutal service
vs:
  hosts:
    - ""
  paths: